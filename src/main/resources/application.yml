server:
  port: 8082

spring:

  datasource:
    url: jdbc:mysql://localhost:3306/e_mart?createDatabaseIfNotExist=true&useTimezone=true&serverTimezone=UTC
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        show_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect

